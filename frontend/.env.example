# Frontend environment variables
# Vite exposes variables prefixed with VITE_ to the browser

# Backend API base URL (default uses Vite proxy, change for production)
VITE_API_BASE_URL=http://localhost:8000
